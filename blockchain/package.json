{
  "name": "scholar-ledger-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Scholar Ledger",
  "private": true,
  "scripts": {
    "migrate:ganache": "truffle migrate --reset --network ganache",
    "migrate:sepolia": "truffle migrate --reset --network sepolia",
    "test": "truffle test"
  },
  "dependencies": {
    "@truffle/hdwallet-provider": "^2.1.15",
    "dotenv": "^16.4.5",
    "ethers": "^6.16.0"
  },
  "license": "ISC"
}
